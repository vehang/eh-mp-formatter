# 2026-04-02 记忆

## 延续待办

- [ ] 执行规范优化（删除重复、添加缺失、更新过时）
- [ ] 完善 USER.md 用户信息（等待用户告知）
- [ ] Docker socket 挂载配置（容器重启后需重新授权）

## 今日工作

### Docker 相关问答

1. **docker-compose 转 docker run** — 转换了 openclaw-easy-v3 的配置
2. **插件警告处理** — feishu-openclaw-plugin 配置残留，无害，可忽略或删除
3. **动态挂载 U 盘** — 讨论了容器运行时挂载新设备的方案：
   - 方案：预留占位目录 + 宿主机 mount --bind
   - 无需重建容器

## 注意事项

- 容器重启后 Docker 权限可能丢失，需执行：
  ```bash
  docker exec -u root openclaw-easy-v3 sh -c 'echo "docker:x:993:node" >> /etc/group'
  ```