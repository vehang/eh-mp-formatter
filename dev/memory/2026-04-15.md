# 2026-04-15 工作记录

## 任务：Hermes Agent Docker 镜像构建

### 背景
接昨日（2026-04-14）工作，创建了 `projects/hermes-docker/` 部署项目，使用南大 ghcr 镜像 `ghcr.nju.edu.cn/astral-sh/uv:0.11.6` 作为 base。

### 分层构建方案
- **Layer 1 (base)**：`Dockerfile.base` — 系统依赖 + Node.js + Python + Playwright chromium
- **Layer 2 (app)**：`Dockerfile.app` — Hermes 源码 + Node/Python 依赖

### 构建进度

| 时间 | 状态 |
|------|------|
| 2026-04-14 晚 | base 镜像构建完成（1.27GB），app 层构建因 ENOSPC 失败 |
| 2026-04-15 早 | 用户清出一部分空间（3.5GB），再次尝试，仍 ENOSPC |
| 2026-04-15 上午 | 用户迁移 Docker 目录到 412GB 大磁盘（274GB 可用） |
| 2026-04-15 11:xx | 多次构建均因 SIGTERM 被中断，Step 7 chown 卡住 |
| 2026-04-15 12:01 | **暂缓** |

### 问题根因
1. **磁盘不足**（已解决，用户扩至 274GB 可用）
2. **SIGTERM 频繁中断**：exec background 任务被系统或用户终止，Step 6 npm install 和 Step 7 chown 均受影响
3. **Step 7 chown 卡住**：`chown -R hermes:hermes /opt/hermes` 在大 node_modules 上因 overlay 拷贝极慢

### 已构建好的
- `hermes-base:latest`（1.27GB）— 完好
- `hermes-agent:latest` — 还未成功构建

### 待办
- 解决 Step 7 chown 慢的问题（考虑用 COPY --chown 或在 base 层做）
- 建议将 Step 6 的 `npm cache clean` 提到单独层，避免缓存污染
- 构建命令：`cd projects/hermes-docker && docker build -f Dockerfile.app -t hermes-agent:latest .`
