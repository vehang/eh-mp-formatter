# 2026-04-05 工作记录

## 今日成果

### 1. OpenClaw 整合版镜像构建

创建了完整的 Docker 镜像构建体系，支持分层构建：

```
openclaw-base:latest (~3GB)
    ↓
openclaw:版本-root (~4GB)
    ↓
openclaw:版本-integrated (~4.7GB)
```

### 2. 构建文件位置

所有构建脚本已迁移到 `openclaw-easy` 项目的 `docker/` 目录：

```
openclaw-easy/
├── Dockerfile.integrated        # 整合版 Dockerfile（根目录）
├── docker/
│   ├── Dockerfile.base              # 基础镜像
│   ├── Dockerfile.version-template  # 版本镜像模板
│   ├── Dockerfile.integrated        # 整合版镜像
│   ├── build.sh                     # 构建脚本
│   ├── init-integrated.sh           # 启动脚本
│   ├── supervisord-integrated.conf  # supervisord 配置
│   └── README.md                    # 构建文档
```

### 3. 修复的关键问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| OPENCLAW_HOME 路径嵌套 | 设置为 `/root/.openclaw`，OpenClaw 追加 `/.openclaw/` | 改为 `OPENCLAW_HOME=/root` |
| --bind 参数无效 | `--bind 0.0.0.0` 不支持 | 改为 `--bind lan` |
| Gateway 启动被阻止 | 未设置 gateway.mode | 添加 `--allow-unconfigured` |
| 挂载覆盖插件 | 挂载卷覆盖运行目录 | 种子目录同步机制 |
| 微信插件未加载 | plugins 配置在 API_KEY 条件内 | 移到条件之外，始终执行 |

### 4. 提交记录

**openclaw-easy (root-version 分支):**
- `4a38f6f` fix: plugins 配置合并移到 API_KEY 条件之外
- `3429df8` fix: nim 插件 source 从 git 改为 path
- `c801306` docs: 更新构建文档
- `8b6ea42` fix: 添加预安装插件的 installs 配置记录

**openclaw-docker-cn-im (main 分支):**
- `48d343f` chore: 添加历史版本 Dockerfile
- `0778b98` fix: 添加预安装插件的 installs 配置记录
- `0d97c8c` feat: 添加整合版镜像构建支持

### 5. 插件配置格式

```json
{
  "plugins": {
    "enabled": true,
    "allow": ["nim", "openclaw-weixin"],
    "load": {
      "paths": [
        "/root/.openclaw/extensions/openclaw-nim-yx-auth",
        "/root/.openclaw/extensions/openclaw-weixin"
      ]
    },
    "installs": {
      "nim": {
        "source": "path",
        "installPath": "/root/.openclaw/extensions/openclaw-nim-yx-auth",
        "version": "1.0.0"
      },
      "openclaw-weixin": {
        "source": "npm",
        "installPath": "/root/.openclaw/extensions/openclaw-weixin",
        "version": "1.0.3"
      }
    }
  }
}
```

### 6. 构建命令

```bash
# 克隆项目
git clone -b root-version https://github.com/vehang/openclaw-easy.git
cd openclaw-easy

# 构建镜像
./docker/build.sh integrated 2026.3.13

# 运行容器
docker run -d --name openclaw \
  -p 18780:18780 \
  -p 18789:18789 \
  -p 18790:18790 \
  openclaw:2026.3.13-integrated
```

### 7. 端口说明

| 端口 | 服务 |
|------|------|
| 18780 | openclaw-easy Web 配置界面 |
| 18789 | OpenClaw Gateway |
| 18790 | OpenClaw Node |

### 8. 插件同步机制

种子目录 `/root/.openclaw-seed/extensions/` 在构建时预装插件，启动时自动同步到运行目录 `/root/.openclaw/extensions/`，解决挂载卷覆盖问题。

---

## 待推送

openclaw-docker-cn-im 的 main 分支有 3 个本地提交未推送：

```bash
cd ~/.openclaw/workspace/dev/openclaw-docker-cn-im
git push origin main
```
